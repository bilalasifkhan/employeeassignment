package com.qts.employeeprojectbackend.daoimpl;

import java.util.ArrayList;
import java.util.List;

import com.qts.employeeprojectbackend.dto.Employee;

public class EmployeeMain {
	
	public static void main(String []args) {
		/*EmployeeDAOImpl employeeDAO = new EmployeeDAOImpl();
		
		List<Employee> employeeList = new ArrayList<>();
		employeeList = employeeDAO.listAllEmp();
		if(employeeList == null) {
			System.out.println("List is null");
		}
		else {
			System.out.println("The size of Array: " + employeeList.size());
		}
		employeeDAO.addEmployee("Aravind DaSilva", 12, 55000);
		System.out.println("The size of Array: " + employeeList.size());
		
		for(int i=0; i<employeeDAO.listAllEmp().size(); i++) {
			Employee e = employeeDAO.listAllEmp().get(i);
			System.out.println(e.getName() + " " + e.getId() + " " + e.getGrade() 
			+ " " + e.getSalary() + " " + e.getIsActive());
		}
		
		employeeDAO.promoteEmployee("EMP10040", 10, 65000);
		System.out.println(" ");
		System.out.println("The size of Array: " + employeeDAO.listAll().size());
		
		for(int i=0; i<employeeDAO.listAllEmp().size(); i++) {
			Employee e = employeeDAO.listAllEmp().get(i);
			System.out.println(e.getName() + " " + e.getId() + " " + e.getGrade() 
			+ " " + e.getSalary() + " " + e.getIsActive());
		}
		
		employeeDAO.updateEmployee("Aravind DaSilva", 1004, 10, 75000, true);
		System.out.println(" ");
		System.out.println("The size of Array: " + employeeDAO.listAll().size());
		
		for(int i=0; i<employeeDAO.listAllEmp().size(); i++) {
			Employee e = employeeDAO.listAllEmp().get(i);
			System.out.println(e.getName() + " " + e.getId() + " " + e.getGrade() 
			+ " " + e.getSalary() + " " + e.getIsActive());
		}
		
		employeeDAO.deleteEmployee(1004);
		System.out.println(" ");
		System.out.println("The size of Array: " + employeeDAO.listAll().size());
		
		for(int i=0; i<employeeDAO.listAll().size(); i++) {
			Employee e = employeeDAO.listAll().get(i);
			System.out.println(e.getName() + " " + e.getId() + " " + e.getGrade() 
			+ " " + e.getSalary() + " " + e.getIsActive());
		}
		
		System.out.println(" ");
		System.out.println("The size of Array: " + employeeDAO.listAllEmp().size());
		
		for(int i=0; i<employeeDAO.listAllEmp().size(); i++) {
			Employee e = employeeDAO.listAllEmp().get(i);
			System.out.println(e.getName() + " " + e.getId() + " " + e.getGrade() 
			+ " " + e.getSalary() + " " + e.getIsActive());
		}*/
	}
	
	

}
